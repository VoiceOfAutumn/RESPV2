import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.pinimg.com', 'media.retroachievements.org'], // Add any other domains here too
  },
};

export default nextConfig;
